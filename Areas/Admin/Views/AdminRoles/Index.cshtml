@model IEnumerable<H2TSHOP2024.Models.Role>

@{
    ViewData["Title"] = "Quản lý quyền truy cập";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-header">
    <h3 class="fw-bold mb-3">H2TSHOP</h3>
    <ul class="breadcrumbs mb-3">
        <li class="nav-home">
            <a asp-action="Index" asp-area="Admin" asp-controller="Home">
                <i class="fa-solid fa-house-user"></i>
            </a>
        </li>
        <li class="separator">
            <i class="fa-solid fa-angle-right"></i>
        </li>
        <li class="nav-item">
            <a href="#">Quản lý hệ thống</a>
        </li>
        <li class="separator">
            <i class="fa-solid fa-angle-right"></i>
        </li>
        <li class="nav-item">
            <a href="#">Quản lý quyền truy cập</a>
        </li>
    </ul>
</div>

<p>
    <a asp-action="Create" class="btn btn-primary">Thêm mới</a>
</p>

<div class="col-md-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Quản lý quyền truy cập</h4>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table id="basic-datatables" class="display table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Tên quyền</th>
                            <th>Mô tả</th>
                            <th>Hành động</th>
                        </tr>
                    </thead>
                    <tfoot>
                        <tr>
                            <th>ID</th>
                            <th>Tên quyền</th>
                            <th>Mô tả</th>
                            <th>Hành động</th>
                        </tr>
                    </tfoot>
                    <tbody>
                        @if (Model != null)
                        {
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.RoleId</td>
                                    <td>@item.RoleName</td>
                                    <td>@item.Description</td>
                                    <td>
                                        <a class="btn btn-primary btn-tone m-r-5" asp-area="Admin" asp-action="Details" asp-controller="AdminRoles" asp-route-id="@item.RoleId">Xem</a>
                                        <a class="btn btn-secondary btn-tone m-r-5" asp-area="Admin" asp-action="Edit" asp-controller="AdminRoles" asp-route-id="@item.RoleId">Sửa</a>
                                        <a class="btn btn-danger btn-tone m-r-5" asp-area="Admin" asp-action="Delete" asp-controller="AdminRoles" asp-route-id="@item.RoleId">Xóa</a>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.5/js/dataTables.bootstrap4.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#basic-datatables').DataTable({
                "paging": true,
                "searching": true,
                "ordering": true
            });
        });
    </script>
}
